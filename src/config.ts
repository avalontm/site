// src/config.ts
const config = {
  title: 'long play record store',
  descripcion: 'Tu tienda en línea con los mejores productos y ofertas.',
  apiUrl: 'https://avalontm.info/api', // Asegúrate de que la URL base esté bien configurada
};
export default config;
